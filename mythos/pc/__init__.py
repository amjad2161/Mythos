"""
mythos.pc — the "run it on your PC" edition.

Environment-file loading (`envfile`) and the environment doctor (`doctor`) that
back `mythos --init` / `--doctor`. Kept together as one concern; the old
top-level module paths remain importable via thin aliases.
"""
